# Changelog

## Unreleased

- 修复插件面板「历史与报告」的筛选按钮：轮询刷新进行中时点击不再被静默丢弃（强制刷新会在在途请求结束后重跑），筛选后显示「筛选中：群 X / 清除」状态，输入框支持回车提交。
- 修复报告「按参与者查看」的长昵称折行：昵称改为单行省略（悬停 title 与右侧详情仍是全名），列表行高恢复一致。

## 0.13.13 - 未发布

- 管理页项目封面与信息分栏、目录折叠、按钮图标和明暗主题统一；固定视口背景光晕，修复底部分层。
- 预检元数据先显示，群列表与缩略图分别加载；忽略关闭后的迟到响应。图片使用内存与磁盘两级缓存，不再阻塞整个预检窗口。
- 快照最多 16 项 / 20 秒；缩略图解码一次一张，内存最多 64 项；磁盘缓存默认 7 天 / 64 MB，可配置。前端缓存最多 60 项 / 60 秒。
- 启动与每小时维护头像、缩略图及自有临时目录。临时目录清理要求归属标记、过期和进程已结束；不扫描系统共享临时目录。历史报告自动删除默认关闭。
- 默认报告目录留空时归入插件数据根；保留已有自定义路径，不自动迁移。原图和投票记录不作为缓存删除。
- 删除旧原型、插画、失效设计图和生成物，测试工具改为动态夹具；重写用户 README，新增使用手册与数据维护说明，归档历史设计文档。

## 0.13.12 - 2026-09-16

- 发布加固：运行时版本和注册版本统一从 metadata.yaml 读取，移除旧 BUILD 和硬编码版本。
- 面板检查不再吞掉页面加载超时，验证目标页面、关键内容与主题；覆盖明暗主题 54 组场景。报告补充明暗主题、三种宽度的页面检查，浅色面板强调色对比度提升。
- 隔离 HTTP 检查对齐当前「已清理」状态并重新验收；修正文档中的 Playwright 依赖安装命令。
- 增加 MIT 许可证、dev/main 开发规范、双仓库发布与回退流程；Git 归档排除原型、开发文档、测试工具，保留完整源码历史。
- 新增干净归档检查：验证发行文件、独立包导入、真实 Pillow 图片派生、目录/单文件报告与 SQLite 重新打开；不替代真实 QQ 或 AstrBot 进程验收。

## 0.13.11 - 2026-09-16

- 管理页移除报告预览：历史表格不再有「预览」按钮，只保留下载（单文件 HTML / 目录 ZIP），后端同时删掉 `reports/preview` 接口与内联预览实现。
- 清理管理页 111 条报告页遗留 CSS（约 535 行），`pages/image-vote/style.css` 从 1320 行降到 763 行；删除前后 27 组面板截图逐字节一致，渲染无变化。
- 窄屏修正：640px 以下历史表格给表格最小宽度、由容器横向滚动，列不再被压成竖排；页面内按钮最小点按高度统一到 40px。
- 新增 `tools/check_panel_ui.cjs`：390/768/1440 × 运行/项目/历史/设置 × 正常/空态/断连共 27 组的布局与脚本检查，支持 `--shots` 输出截图；报告侧继续用 `tools/check_report_ui.cjs`。
- 元数据补齐：`metadata.yaml` 增加 `tags`，仓库根新增 256×256 `logo.png`，具备提交 AstrBot 插件市场的字段。
- 文档对齐到当前版本：`IMPLEMENTATION_STATUS.md` 重写（140 项测试、两个浏览器检查、真机 AstrBot 4.28.0 场次、未验收项），`ARCHITECTURE.md` 修正 Reply / get_msg 事实与「下一步」，`ROADMAP.md` 标注为历史路线图，`AGENT_CONTEXT.md` 同步到 0.13.11。
- 删除已无引用的 `templates/report.html.j2`。

## 0.13.10 - 2026-09-16

- 柱状图按卡片可用宽度重算横坐标，固定绘图区高度与字号，消除宽卡片中央的大面积留白。
- 默认每个人物保留一张高清封面，其余仅缩略图；新增 `report_image_policy`，可切回全部高清，原图不修改。
- 单文件导出和网页预览共用去重资源池，移除图片在 JSON 与兜底 HTML 中的重复内嵌；详情标明缩略图，单文件无脚本时保留文字统计。

## 0.13.9 - 2026-09-16

- 人物详情的最终参与者评分按分数分组，头像悬停 / 聚焦显示昵称，点击进入参与者详情；修复弹窗通用图片样式导致头像撑满的问题。
- 详情与个人页柱状图统一使用紧凑宽度和概览比例，避免随宽面板放大。
- 默认 AI 提示词直接填入配置与设置页，恢复默认会填回完整内容；模板生成与配置默认值同源。
- 删除已废弃的固定周期开关、配置字段、对应测试和发送等待函数的无用耗时参数；人物等待仍从发送完成后开始。

## 0.13.8 - 2026-09-16

- 报告头图优先使用当前排名第一人物的图片，无评分时回退展示图；缩短头部并增强浅色主题，高分采用金色标识（配置评分区间上 20%，页面展示阈值）。
- 全部图片支持图册 / 人物列表切换、筛选与排序；人物详情包含图片和最终参与者评分，图片详情支持人物内部前后切换。
- 概览解读改为数据速览、AI 一句话和可展开观察；保留旧纯文本总结。默认 AI 提示词改为有证据的数据解说，输入扩展为全人物分布、中位数、标准差、极值、票数覆盖和小样本标记；不发送参与者个人明细。
- 结构化 AI 结果为报告数据的可选附加字段，无数据库迁移；无脚本报告降级为可读文本，自定义提示词继续保留。普通重导出复用旧总结，显式重新生成 AI 才使用新默认提示词。

## 0.13.7 - 2026-09-16

- 报告视觉升级为默认深色的作品报告面板：背景封面、紫色高亮、奖牌高分人物卡、紧凑图册与带封面缩略图的完整排名；保留浅色切换与窄屏布局。
- 新增离线 SVG 评分柱状图、人物获票率环图、参与者覆盖率与评分分布；多人场次支持个人评分和本轮均分的并排条形比较，单人场次不重复比较。
- 图表根据最终人物票生成，不新增图片评分、趋势或偏好推断；支持配置评分区间、零票、空数据和键盘可读数据。无需第三方图表包或外部 CDN。
- 本地已跑通无截图交互回归（深色默认主题、柱状图分箱、获票率环图、多人比较、0-100 与 0-0 评分区间等边界）与 133 项 Python 测试；视觉与图片验收以重新导出的成品报告为准。

## 0.13.6 - 2026-09-16

- 离线报告概览增加项目图片头部、完整人物排名与单人评分提示；保留人物票口径和少票状态。
- 全部图片改为紧凑人物图册，默认展示前三张，支持展开和搜索；图片不显示独立评分。
- 参与者页改为可排序的最终人物评分表，展示实际来源图片与缺失状态，说明覆盖率和本轮均分口径。
- 修复搜索重绘丢失焦点，补充导航、展开、输入与表格的可访问标记，以及图片加载失败提示。
- 新增 `tools/check_report_ui.cjs` 交互回归检查（需 Playwright 和 Chrome），覆盖排序、搜索、隐私、空状态等；本地用 playwright-core + 系统 Chrome 跑通（PASS），Python 测试 133 通过。图片与视觉验收由用户重新生成报告后完成。

## 0.13.5 - 2026-09-15

- 合并发送不再按整条消息刷「超过设定间隔」：真机跑完整一场（45 张 / 17 人物 / 22 条消息）报了 12 条这种 WARN，因为一条消息里 2-3 张图的上传耗时必然超过人物间隔。现在逐张模式仍按单张判定并 WARN，合并模式改按「平均每张」判定，只有平均每张都追不上间隔才 WARN，文案里同时给出整条耗时与平均耗时。
- 报告概览的人物卡片布局修正：排名卡片加 2 列数据栅格、封面图溢出与长名字换行处理，窄屏 950px 起两列、手机单列。
- 测试 130 → 133（新增合并/逐张两种节奏判定的用例）。

## 0.13.4 - 2026-09-15

- 投票消息改成名字打头：首行只放「项目 · 人物」，第二行进度，第三行打分方法，引用定位标记移到末行「[投票 003/045 · B7061F3F]」；群里第一眼看到的是作品名与人物名，不再是编号与短 ID。
- 依据：AstrBot 的 aiocqhttp 适配器收到 reply 段会用 get_msg 取回被引用消息的完整文本与 chain，解析扫描整段文本，标记放末行不影响引用改分。
- 打分提示合并为一行「回复 0-10 给「Elis」打分 · 引用本场任意图片可改分」。
- 引用定位用例更新为末行标记：末行标记仍能定位到对应图片，跨场次标记仍被拒绝。测试 130。

## 0.13.3 - 2026-09-15

- 群内投票消息重排：首行「【投票 012/045 · 短ID】项目 · 人物」，第二行「第 2/17 位人物 · 第 3/7 张」，打分提示单独成行。人物还没发完时写明「之后还有 N 张」，最后一条写「本人物已发完」，逐张与合并发送都能一眼看出哪条是中继、哪条收尾。
- 合并发送拆条时第二行给出「第 4-6/7 张（本条 3 张）」，不再需要数图片。
- 首行定位标记从 `[投票 003/045 · A5935BC8]` 改为 `【投票 003/045 · A5935BC8】`：引用解析同时接受两种括号，旧场次已发出的图片仍然可以引用改分。
- 状态改中文：`/vote status`、pause/resume/finish/stop 回复与报告页的状态统一显示「轮播中 / 已暂停 / 正在结算…」，不再出现 `RUNNING` 等枚举值。
- 控制指令回复补「当前人物：Elis（已收 3 票）」，进度改「7 / 19 张（第 2/2 位人物 · 该人物第 3/7 张）」；`/vote status` 的「下一张」改为「下一步」，合并模式下给出下一组的张数区间与倒计时。
- `/vote <项目名>` 的开始回复新增「发送方式」一行（逐张发送 / 合并发送，含每条上限）；`/vote reloadconfig` 同步显示发送方式。
- 自动暂停文案按消息计数：合并发送时失败与重试都以「条消息」为单位（一条可能含多张图），日志、群提示与 `session.error_message` 同步改成中文。
- 面板「报告状态」把「未生成或已清理」拆成「无报告」与「已清理」；运行卡片说明改为「同一个人物就是一个投票窗口」。
- 配置页说明：`interval_includes_send_time` 标注为已废弃且不再生效，`max_send_retries` / `send_failure_pause_threshold` 改为「条消息」，`score_min` 提示补「填 0 表示允许 0 分」。
- 报告页「按人查看」改为「按参与者查看」，人物未获票徽标改为「已展示，暂无评分」，参与者页指标改为「已评分人物 / 该参与者均分 / 人物覆盖率」，报告头部「部分结果」改为「图片未全部展示」。
- 插件重载或重启导致的暂停在面板里改为中文说明。测试 123 → 130。

## 0.13.2 - 2026-09-15

- 修复合并发送把连接打挂：一条消息里塞 7 张图（单个 WebSocket 帧约 48MB base64）会让 NapCat/QQ 直接断开连接，发送调用永远不返回、整场卡死（真机复现：合并发送第 2 个人物时连接中断两次，日志里没有任何报错）。
- 新增 `merge_character_images_max`（默认 3）：一个人物的图片超过该数量时拆成多条消息发送，人物投票窗口不变。
- 新增 `send_timeout_seconds`（默认 90）：单条消息超过该时间没有返回就按发送失败处理，避免连接异常时整场卡死；超时会在日志与 `session.error_message` 写明「发送超时」。
- 测试 121 → 123。

## 0.13.1 - 2026-09-15

- 新增配置 `merge_character_images`（默认关闭）：开启后，同一个人物的多张图**合并成一条消息**发送（一条消息里多张图），回复一次分数即给该人物打分；关闭时保持现状（逐张发送、同一人物内部不等待）。
- 合并发送的耗时接近这些图片上传时间的总和，省掉的是逐条消息与逐张之间的开销；人物之间的等待间隔不变。
- 发送链路改为按「一组图片」传递：消息构造、重试、发送状态标记都以组为单位；合并模式下整组失败计一次连续失败，仍受自动暂停阈值保护。
- 测试 120 → 121。

## 0.13.0 - 2026-09-15

- 投票主键从图片升级为人物：同一用户对同一人物只保留一票；引用本场任意图片时，评分归属该图片所属人物。旧测试数据在部署时清空，不承诺把历史图片票自动合并为业务结果。
- 启动前按人物首次出现顺序稳定分组，人物内部保持原扫描顺序；同一人物图片连续发送且不等待，首张成功发送后立即接收人物票，最后一张发送完成后等待完整人物间隔。
- 最后一个人物同样获得完整投票窗口；某人物全部图片发送失败时不会开放普通数字投票，部分成功时仍可正常投票。
- 报告数据升级到 schema v3：概览按人物排名，「全部图片」按人物归类，逐人页每个人物只显示一张最终票；图片不再单独计票或排名。
- 会话和票据新增 `active_character`、`character_count`、`character_name`，SQLite 启动迁移会回填人物并折叠重复历史票；评分下限为 0 时支持 `0` 与 `0分`。
- 插件工作区、群内状态、预检、配置说明和 AI 统计统一改为人物口径。单元测试 116 → 120。

## 0.12.11 - 2026-09-15

- 新增彻底删除：`/vote purge <session_id|短ID> confirm` 会删除该场次的报告目录**以及数据库里的投票记录（票与候选）**，不可恢复；原图与群聊消息不受影响。运行中或收尾中的场次会拒绝删除。
- 插件工作区「历史与报告」表格新增「彻底删除」按钮（带二次确认），接口 `sessions/purge` 需要 `confirmed: true`。
- 原有 `/vote cleanup` 与页面「清理」保持不变：只删报告文件、保留投票记录。
- 测试 114 → 116。

## 0.12.10 - 2026-09-15

- 展示标题剥掉采集流水线标记：`screenshot0020 - Vess - 30279726 - reset-1.png` 现在显示为 `Vess`（尾部哈希、`reset` / `reset-N`、长数字段依次剥掉，剥到只剩一个词为止；普通文件名与 `Grace-现代版本` 这类标题不受影响）。角色名同步受益。
- 报告「全部图片」页最外层新增汇总行：共 N 张图 · 已展示 M 张 · X 张有效票 · K 位参与者 · 其中 Y 张有票。
- 报告「按人查看」页最外层新增汇总行：K 位参与者 · 共 N 张图 · 个人评分合计 X 张。
- 投票人名字改回群名片优先（名片为空才退回昵称）：群里显示的是群名片，之前按昵称取会让报告与群里不一致（真机案例：名片「雾舞(朱红轻飞溅)」被显示成昵称「雾舞骛坞雾吾屋」）。
- 测试 113 → 114。

## 0.12.9 - 2026-09-15

- 修复「@别人 + 数字」被静默丢弃：AstrBot 会把 @其他用户 拼成 `" @昵称(qq) "` 塞进 message_str，于是 `@某人 7` 不是纯数字，连忽略日志都不会产生（真机案例：妖桃引用用户消息并 @ 对方后发 7）。现在解析前先去掉这段 @ 片段，`@某人 7`、`@某人 8分` 都能识别；引用非本场图仍按规则忽略，但会留下日志。
- 测试 112 → 113。

## 0.12.8 - 2026-09-15

- 新增明确的截止符号：轮播结束、进入结算时在群里发一条「⏹ 投票截止：<项目>／已发送 N/M 张／此刻起数字与引用票不再计入，正在结算…」，取代原先只在结算完成后才出现的提示。
- 截止之后彻底不处理群消息：结算中（FINALIZING）与准备中（PREPARING）的场次一律跳过，不再读候选、不再记票、也不再写忽略日志；`/vote` 系列指令走独立入口，不受影响。
- 结算完成的通知改为「📊 结算完成：<项目>／有效票 X／参与 Y 人」，与截止符号分工明确。
- 测试 111 → 112。

## 0.12.7 - 2026-09-15

- 群消息热路径短路：没有未结束场次、又不在白名单里的群直接返回，不再每条消息都解析配置和查库（原先每条群消息都会做一次 SQLite 查询）。
- 消息路径上的配置检查最多每秒一次；指令路径仍即时读取，`/vote reloadconfig` 行为不变。
- 只缓存「本群没有场次」这一结论（5 秒）；场次一开始就在内存活跃表里，不受缓存影响，不会漏票。
- 新增两条回归测试（无场次的群只查一次库、白名单外的群完全不查库），测试 109 → 111。

## 0.12.6 - 2026-09-15

- 评分放宽一种写法：`8` 与 `8分` 都算有效票（全角数字、数字与「分」之间的空格一并归一化）；`8分熟`、`打了8分`、`分8` 这类句子仍不算，避免误判。
- 投票提示语同步改为「回复 1-10 评分（如 8 或 8分）」。
- 测试 108 → 109。

## 0.12.5 - 2026-09-15

- 投票人名字改为优先 QQ 昵称、其次群名片：AstrBot 转换层用的是「名片 or 昵称」，群里常见把名片设成符号、昵称才是常用名（真机案例：名片「。」、昵称「黑白星」），报告里应显示昵称。
- 回归测试同步更新语义：昵称优先、名片回退、没有原始事件时退回 AstrBot 的取值。

## 0.12.4 - 2026-09-15

- 修复单文件报告发不到群里：`send_file` 的降级分支把裸组件列表交给平台发送接口，触发 `list object has no attribute chain`，报告附件静默失败（群里只看到"报告已生成"，附件没来）。现在统一组装成 MessageChain。
- 发送图片与纯文本的降级分支同样改为 MessageChain，避免同一类问题；确实不支持文件组件时抛出明确错误而不是静默降级。
- 新增适配层回归测试（断言传出对象是 MessageChain、组件顺序为文本+文件），测试总数 107 → 108。

## 0.12.3 - 2026-09-15

- 修复插件日志不进面板：AstrBot 4.27+ 的 `astrbot.api.logger` 是按调用方动态解析的代理对象，不是 `logging.Logger`，原先的 isinstance 判断失败后退回普通 logger，INFO 级日志（计票、忽略原因、迁移告警）全部丢失。
- 现在优先用 `LogManager.get_plugin_logger` 取插件专用 logger，它挂着面板的日志队列 handler；取不到再退回标准 logger，保证本地测试可用。
- 新增 logger 解析的单元测试（模拟 AstrBot 环境与无 AstrBot 的本地环境），测试总数 105 → 107。

## 0.12.2 - 2026-09-15

- 修复老库 10 分票被数据库丢弃：`votes` 表残留 `CHECK(score BETWEEN 0 AND 9)`，1-10 分制下打 10 分的票会触发约束错误、整票静默丢失（真机日志实证：一场 15 张的投票丢了 7 票）。SQLite 无法修改 CHECK，启动时按当前上限重建该表，历史票与唯一索引原样保留。
- 修复候选角色从未落库：创建会话时重建候选对象漏了 `character` 字段，`character_name` 一直为空，角色合并统计只能靠标题临时兜底；带 `project.json` 角色映射的项目会统计错。
- 投票人名称改为优先取**群名片**（群里显示的那个名字），取不到再回退 QQ 昵称；此前报告里显示昵称，与群里看到的不一致。
- 新增投票对账日志：每条计票记录「第几张 / 标题 / 分数 / 来源 / 是否覆盖旧票」，被忽略的数字记录原因（超出评分范围、前导零、引用不是本场投票图、本场不在收票状态、还没有已发送图片）。
- 测试从 100 增加到 104。

## 0.12.0 - 2026-09-13

- 新增 Plugin Pages 工作区：运行控制、项目登记与目录浏览、预检、历史报告、预览和 HTML/ZIP 下载、分组配置保存与版本冲突检查。接口要求当前 Dashboard 管理员身份，卸载时释放本实例的路由和导出任务。
- 总结报告升级为概览、图片图册/列表、评分详情、按人查看三层结构；支持明暗主题、分歧排序、少票/零票/未展示/发送失败状态和离线阅读。保留原有角色汇总。
- 报告数据版本升为 2，加入最终逐人评分、总体标准差和以成功展示图片为分母的覆盖统计。汇总分享模式在序列化时移除个人明细与群号。
- 新增 QQ 头像派生缓存，限制下载大小、尺寸和并发，失败使用姓名首字；单文件报告内嵌头像。
- AI 提示词支持变量和预览；新增 `/vote export --ai` 显式重新生成总结，模型调用超时降级。
- 报告重新导出先生成临时产物，失败保留旧报告；报告失败在历史中可见，清理后仍可从保存的评分重新生成。
- 补齐重启后暂停场次的结束/取消操作与网页倒计时。新增隔离 HTTP 验收及大图验证工具；验证范围见 `docs/IMPLEMENTATION_STATUS.md`。
- 修复工作区的并发与持久化缺陷：并发导出只启动一次生成；配置保存失败时把配置文件回滚到保存前的内容；下载与预览期间拒绝清理报告，单文件下载先打开句柄再流式返回；项目改名在一次写入里完成；报告读取、生成与清理的互斥统一到应用层守卫，群命令与网页共用（`/vote cleanup` 不再能删掉正在下载或生成的报告）。
- 修复报告页面在手机宽度下的横向溢出：按人查看页的图片网格改为 minmax(0, 1fr)，侧栏与详情允许收缩。

## 0.11.0 - 2026-09-13

- `same_user_vote_policy` 扩展为四种：`last_wins`（默认）、`first_wins`、`max_score`、`min_score`；语义仍是「同一用户对同一张图只保留一票」，策略只决定取值，来源与 message_id 跟随胜出的那一次。
- 新增角色维度统计：候选带 `character`（默认取展示标题第一个短横线之前的部分），`project.json` 的 `characters` 可覆盖；统计、报告与 AI 输入都带角色合并结果。
- 报告在发生合并时多渲染「角色汇总」表格；`/vote check` 增加角色数量行。
- 老数据库自动为 candidates 补 `character_name` 列。
- 测试从 71 增加到 75。

## 0.10.1 - 2026-09-13

- 新增 `interval_includes_send_time`（默认关）：开启后按固定周期发送，等待时间 = max(0, 间隔 − 本张发送耗时)，消除「设置 5 秒、实测约 20 秒」的差值（差值是图片上传耗时）。
- 每张图片发送耗时写入 DEBUG 日志；耗时超过设定间隔时打 WARNING 并提示可开启周期模式。
- 单文件报告成功后清理 `report.css`、`report.js` 与 `images/`，报告目录只保留 `index.html`、`data.json` 与 marker。
- 测试从 70 增加到 71。

## 0.10.0 - 2026-09-13

- 修复「WebUI 里改了发送间隔 / 报告模式但没生效」：配置解析优先读 `astrbot_config_mgr` 与磁盘上的插件配置文件（WebUI 保存后即更新），过期来源排在后面；文件按 (mtime, size) 缓存，下一条指令生效。
- 新增管理员指令 `/vote reloadconfig`：重新读取配置并打印实际生效值（间隔、评分范围、报告模式、结束开关等）与配置来源。
- 新增 `send_report_html`（默认关）：`report_mode = single_html` 时把单文件报告作为附件发到群里。
- 报告生成时记录实际使用的模式。
- 测试从 67 增加到 70。

## 0.9.3 - 2026-09-12

- 控制指令（pause / resume / finish / stop）回复的末行改为「项目：X · Session：Y · 状态：Z」，每条回复都能独立看出属于哪个项目。
- 老会话自动纠正项目名：运行或导出时如果 `project_path` 命中登记表，会话项目名改成登记名并落库，报告目录随之统一到项目名下。
- 修复 `save_session` 的 UPSERT 不更新 `project_name` 的问题（改名无法持久化）。
- 测试从 66 增加到 67。

## 0.9.2 - 2026-09-12

- 投票消息不再重复短 ID：第二行改为「项目名 · 图片标题」，冗余的 `[VOTE:短ID:序号]` 机器标记不再发送；解析器保留对历史消息的兼容。
- 修复登记项目（以及别名项目）报告目录使用文件夹名的问题：会话改用调用时使用的项目名，报告目录随之从 `人物图` 变为 `海滨之家`。
- 测试从 65 增加到 66。

## 0.9.1 - 2026-09-12

- `/vote pause`、`/vote resume`、`/vote finish`、`/vote stop` 现在都会在群里回复确认消息：触发的是哪条指令、项目名、进度（已发 / 总数）、会话状态，以及下一步提示。
- 没有进行中或可恢复的会话时，回复改为明确的中文说明，而不是异常文本。
- 测试从 63 增加到 65。

## 0.9.0 - 2026-09-12

- 新增 `notify_on_finish`（默认开）：投票结束时在群里发送提醒，含项目名、图片数、已发送张数、有效票与参与人数；报告生成成功或失败会再补一条。
- 新增 `auto_report_on_finish`（默认开）：关闭后会话照常结束但不生成报告，由管理员执行 `/vote export` 手动导出。
- 配置页新增「结束通知与自动报告」分组；两个开关只影响收尾行为。
- 测试从 62 增加到 63。

## 0.8.0 - 2026-09-12

- 新增项目登记表 `projects.json`：项目名 → 容器内任意绝对路径，支持跨卷与任意层级，解析优先级高于别名与 `input_root`。
- 新增管理员指令 `/vote register`、`/vote unregister`、`/vote projects`；`/vote list` 分开列出注册项目与目录项目。
- 登记项可覆盖 `interval_seconds`、`recursive`、`description`；文件按修改时间自动重载，外部编辑无需重载插件。
- `/vote check` 对登记项目只显示名称，绝对路径仅在管理员的 `/vote projects` 中呈现。
- 测试从 58 增加到 62。

## 0.7.0 - 2026-09-12

- 新增 `send_failure_pause_threshold`（默认 3，0 表示关闭）：连续发送失败达到阈值时自动暂停会话，并向投票群发送提示，管理员可用 `/vote resume` 继续。
- 短 ID 从 4 位十六进制提高到 8 位（约 43 亿种），降低跨会话引用误路由的概率；4 位的历史会话仍能正常解析。
- 测试从 55 增加到 58。

## 0.6.0 - 2026-09-12

- 修复插件卸载/重载会把运行中的会话写成 `CANCELLED`、导致无法续跑的问题：现在留下 `PAUSED`，`/vote resume` 可继续；显式 `/vote stop` 仍然是 `CANCELLED`。
- `FINALIZING` 纳入恢复范围：结算中断的会话重启后可重新结算并生成报告。
- 新增 `active_candidate_id`：发送失败的图片不再成为投票目标，窗口票落在最近一次成功发送的图片上；老数据库自动补列。
- 报告改为在 `COMPLETED` 之后生成，`finished_at` 先于结算写入；报告失败只记录错误，保留 `/vote export` 重试路径。
- 计票范围以会话快照为准：运行中修改评分范围只影响新会话，并打印一次警告。
- 监听入口跳过机器人自己发出的数字消息。
- 测试从 46 增加到 55。

## 0.5.0 - 2026-09-12

- 评分制可配置：`score_min` / `score_max` 上限放宽到 100，支持 1-5、1-10 等满分制；解析位数由范围决定，支持两位数，新增 NFKC 归一化（全角数字可用）并拒绝前导零。
- 群消息提示语、报告分数标签、`/vote check` 输出全部跟随配置，不再写死 1-4。
- session 持久化评分范围，老数据库自动补列，历史报告按当时的范围渲染。
- AI 输入与提示词带上评分范围。
- 修复 `single_html` 模式从 `data.json` 读回后分数分布键变成字符串、导致分布显示为 0 的问题。

## 0.4.0 - 2026-09-12

- 按目标实例实测（AstrBot 4.27.5）重写兼容层：改用 `StarTools.get_data_dir`，`filter` 与 `EventMessageType` 按多种真实布局探测，装配日志自报命中来源。
- 修复插件以 `data.plugins.<插件名>.main` 包形式加载时的导入失败，并加入回归测试。
- `_conf_schema.json` 改为官方对象格式（分组、hint、options），配置读取同时兼容分组与扁平结构。
- 接入 AI 统计总结（`get_current_chat_provider_id` + `llm_generate`，失败不影响报告生成），AI 输入新增争议项。
- `/vote finish` 立即停止后续发送；`/vote cleanup` 支持短 ID 与报告目录名；新增按保留天数自动清理。
- `/vote check` 补齐命名统计、首末各 5 张、非法文件、预计耗时与体积估算；`/vote status` 增加倒计时；启动回复增加预计耗时。
- 报告默认按排名排序并可切回原始顺序，头部增加开始/结束时间，发送失败的图片单独标注。
- 日志按 INFO / DEBUG / WARNING / ERROR 分级接入。
- 新增 `docs/ROADMAP.md`：差距清单、新需求与 AstrBot 官方约定核对结果。

## 0.1.0 - 2026-09-11

- 建立配置、领域模型、项目扫描、路径安全、投票路由、统计、SQLite、报告和任务生命周期边界。
- 加入 AstrBot 可选导入兼容壳，核心模块不依赖 AstrBot。
- 加入 Python 标准库可运行的核心回归测试。

## 0.2.0 - 2026-09-11

- 接入可暂停、可恢复、可停止和可提前完成的轮播 runner。
- 接入群消息投票、引用投票路由、重启后暂停恢复和最近 session 导出。
- 接入 AstrBot MessageChain/Reply 适配边界与安全报告清理命令。
- 新增应用层、适配层和 runner 的回归测试。

## 0.3.0 - 2026-09-11

- 完善 `/vote status`、`export`、`cleanup`，并要求清理全部报告时显式确认。
- 支持目录报告与压缩图内嵌的 single HTML 模式，超限自动回退目录模式。
- 接入结构化 AI 总结输入、项目别名、重启后暂停投票和报告输出目录防碰撞。
- 增加真实 AstrBot API 探针 `tools/astrbot_api_probe.py`。
