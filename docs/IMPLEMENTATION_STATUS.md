# 实现与验收状态

> 更新时间：2026-09-16。已发布基线：**0.13.12**；当前为 **0.13.13（未发布）** dev 修订。本文件是「当前实现到哪、验收到哪」的出处；逐版本变更见 [CHANGELOG.md](../CHANGELOG.md)，需求基线见 [REQUIREMENTS.md](REQUIREMENTS.md)，历史差距表见 [ROADMAP.md](ROADMAP.md)。

## 一、结论

插件在真机（AstrBot 4.28.0 + NapCat）上已完成多场完整投票：发送、计票、统计、报告与清理的主单位都是**人物**，同一人物多图连续或合并发送，每人每人物只保留一张最终票。

- 本地 **151 项单元测试**全部通过（带 Pillow 的 Python 3.12 环境，无跳过）。
- 报告页与插件面板各有可复跑的浏览器检查：`tools/check_report_ui.cjs`（DOM 与交互）、`tools/check_panel_ui.cjs`（390/768/1440 × 四页 × 正常/空态/断连，明暗主题共 54 组）。
- 真机已完成：45 张 / 17 人物 / 46 票完整场次；合并发送拆条；自动暂停与恢复；彻底删除；报告导出与下载。

## 二、当前能力

### 发送与计票

- 扫描后按人物首次出现顺序分组，人物内部保持原顺序；同一人物图片连续发送（默认）或按组合并发送（每条默认最多 3 张，超出拆条）。
- 人物第一张成功发出即开放普通数字票；最后一张发完才等待完整人物间隔；最后一个人物使用最终等待时间，且不短于人物间隔。
- 普通数字票归属当前人物；引用本场图片归属该图人物；每人每人物一票，默认后票覆盖。
- 连续发送失败达到阈值自动暂停并在群里提示，`/vote resume` 继续；发送超时按失败处理。

### 报告

- 默认深色作品面板（可切浅色）：封面、奖牌高分卡、完整排名表、获票覆盖条、紧凑图册 / 人物列表切换、搜索与筛选。
- 离线 SVG 图表：评分分布柱状图、人物获票率环图、参与者评分分布与覆盖率；多人场次给出个人评分与本轮均分比较。
- 三种视图：概览 / 全部图片 / 按参与者查看；图片只作为人物素材，不单独计票或排名。
- 导出模式 `directory` 与 `single_html`；关闭参与者明细可生成汇总分享版；`report_image_policy` 默认每个人物只保留一张高清封面。

### 插件工作区（Plugin Pages）

- 四个页面：运行 / 项目 / 历史与报告 / 设置。启动与预检、暂停恢复、目录登记与浏览、历史场次、报告下载与重新生成、清理与彻底删除、分组配置保存（含版本冲突）。
- 历史报告提供**下载**（单文件 HTML 或目录 ZIP）；报告预览已按用户要求移除，改为直接下载。
- 窄屏已验收：见下方验证方式表。

### 配置与元数据

- 配置写回同一份 AstrBotConfig，支持分组与扁平结构；评分范围 0–100、合并发送、超时、结束行为、AI 提示词模板等全部在配置页可编辑。
- `metadata.yaml` 含 `repo` / `astrbot_version` / `support_platforms` / `tags` 与 256px `logo.png`，具备提交插件市场的字段。

## 三、验证方式与最近结果

| 方式 | 命令 | 最近结果（2026-09-16） |
| --- | --- | --- |
| 单元测试 | `PYTHONDONTWRITEBYTECODE=1 python3 -m unittest discover -s tests -t .` | 151 项通过，无跳过 |
| 报告页检查 | `NODE_PATH=<playwright> node tools/check_report_ui.cjs` | PASS：排名、零分、转义、展开、搜索焦点、弹窗、来源、排序、主题、隐私与空态 |
| 面板检查 | `NODE_PATH=<playwright> node tools/check_panel_ui.cjs [--shots <dir>]` | PASS：明暗主题 54 组页面无横向溢出、无脚本错误 |
| 隔离 HTTP 验收 | `python tools/check_workspace.py --astrbot-source <AstrBot 检出>` | 0.13.12 重新运行 6 组断言通过；真实 SQLite/Pillow，群发送与模型为替身 |
| 大图处理 | `python tools/check_large_report.py` | 7 张 5000×5000 PNG，原图 SHA-256 未变（历史记录） |
| 真机场次 | NAS 实例（AstrBot 4.28.0 + NapCat） | 45 张 / 17 人物 / 46 票；报告生成、下载与重新导出均正常 |

两个浏览器检查都需要 Playwright：本机可 `npm install --prefix /tmp/pwcheck playwright` 后用 `NODE_PATH=/tmp/pwcheck/node_modules` 运行（脚本使用系统 Chrome，不下载浏览器）。

## 四、尚未验收

| 项目 | 原因 |
| --- | --- |
| 单文件报告 `file://` 双击打开 | 需人工双击确认；无头浏览器已渲染通过 |
| 真实头像下载 | 头像服务用替身响应测试，未访问真实头像接口 |
| 跨进程重启 E2E | 恢复逻辑由单元测试覆盖，未做进程级演练 |
| 500 MB 真实照片项目 | 合成大图已验收，未跑真实照片 |
| 多群并行 | 双群隔离在隔离验收中覆盖，未在真机并行开两场 |
| AstrBot 4.4.x | 目标实例为 4.28.0，未验证更早版本 |

## 五、已知取舍

| 条目 | 现状 |
| --- | --- |
| 发送重试退避 | 实现为 2 / 4 / 6 秒（需求写 2 / 5 / 10） |
| 单文件超限回退通知 | 写日志与报告页说明，不回群消息 |
| 项目内 `project.json` 的 `name` / `sort_mode` | 未实现；支持 `files` 顺序与 `characters` 映射 |
| 扫描忽略报告输出目录 | 未实现；`output_root` 放在项目目录内时会被扫到 |
| 报告重新导出 | 同一场次同一时间只允许一次导出；旧报告是静态快照，新样式不会自动回溯 |

## 六、历史修复记录（0.12.x–0.13.x）

以下问题均已复现并闭合，保留作为回归背景：

1. **并发导出竞态（0.12.2）**：导出先占位再校验，重复请求只放行一个。
2. **配置保存失败未回滚磁盘（0.12.2）**：保存抛错时同时还原内存与磁盘。
3. **下载 / 预览与清理竞态（0.12.2）**：读取、生成、清理三种状态由 `ReportActivity` 互斥。
4. **改名非原子（0.12.2）**：登记新名与移除旧名合并为一次写入。
5. **报告手机宽度溢出（0.12.2）**：`.report-grid` 改 `minmax(0, 1fr)`。
6. **群命令清理绕过读取保护（0.13.0）**：`/vote cleanup` 与页面共用同一个守卫。
7. **票表评分上限残留（0.12.2）**：`votes` 表残留 `CHECK(score BETWEEN 0 AND 9)` 导致 10 分票被数据库拒绝、静默丢失；启动时检测并重建表。
8. **合并发送把连接打挂（0.13.2）**：一条消息塞 7 张图会让 NapCat 断开；新增每条约 3 张上限与 90 秒发送超时。
9. **合并发送节奏警告刷屏（0.13.5）**：改按平均每张判定，避免每个多图人物刷一条 WARN。
10. **报告视觉与图册（0.13.6–0.13.10）**：深色面板、离线 SVG 图表、图册/列表切换、参与者评分表、封面策略与单文件资源去重。
11. **面板整理（0.13.11）**：移除报告预览（改为下载）、删除 111 条报告页遗留 CSS（渲染逐字节不变）、窄屏历史表格与按钮点按尺寸修正。

## 当前 dev 的发布前修订

- 预检先返回元数据，缩略图逐张加载，群列表独立就绪；关闭后迟到请求不再重开弹窗。
- 后端缩略图增加 7 天 / 64 MB 可配置磁盘缓存，前端缓存 60 条 / 60 秒；新配置报告输出留空时集中到插件数据根。
- 删除旧原型、插画和生成报告；工具改为现场生成测试图片，不依赖大型素材。README 改为面向用户的上手入口，技术细节移到使用手册和数据维护文档。
- 长时间真实负载下的内存曲线尚未测量，不能声称绝无泄漏；数据库和主动保留的报告不设自动硬上限。现有自定义目录不搬迁，未做新的生产部署。

本轮额外验证：预检在群列表与缩略图延迟时先显示信息、群列表就绪即可开始、关闭后迟到响应不重开；跨服务重建复用磁盘缩略图，容量清理保留非缓存文件。运行文件单独复制到临时目录后可导入并生成目录/单文件报告，不依赖旧原型素材。尚未做本轮真实 QQ 或长期内存压测。
